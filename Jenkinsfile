pipeline {
  agent any
  stages {
    stage('Static Code Validation') {
      steps {
        echo 'Static Code Validation'
      }
    }
    stage('Lint Validation') {
      steps {
         echo 'Lint Validation'
       }
     }								
  }
}